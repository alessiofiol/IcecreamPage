import { FilterespecialPipe } from './filterespecial.pipe';

describe('FilterespecialPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterespecialPipe();
    expect(pipe).toBeTruthy();
  });
});
