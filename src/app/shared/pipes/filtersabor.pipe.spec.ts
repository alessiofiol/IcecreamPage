import { FiltersaborPipe } from './filtersabor.pipe';

describe('FiltersaborPipe', () => {
  it('create an instance', () => {
    const pipe = new FiltersaborPipe();
    expect(pipe).toBeTruthy();
  });
});
